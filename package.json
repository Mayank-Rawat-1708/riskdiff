{
  "name": "riskdiff",
  "version": "0.1.0",
  "private": true,
  "description": "A transaction risk policy workbench built on GitAgent / OpenGAP.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "install:all": "cd server && npm install && cd ../web && npm install",
    "build": "cd web && npm install && npm run build && cd ../server && npm install && npm run build",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "test:lifecycle": "node scripts/lifecycle-test.mjs",
    "test:smoke": "bash scripts/smoke.sh"
  }
}
